# Test file for validating the new code review functionality

def test_function():
    # This is a test function with some potential issues
    password = "hardcoded_password"  # Security issue
    
    # Inefficient loop
    result = []
    for i in range(1000):
        for j in range(1000):
            result.append(i * j)
    
    # Missing error handling
    file = open("test.txt")
    data = file.read()
    
    return data

def another_function():
    # SQL injection vulnerability
    query = "SELECT * FROM users WHERE id = " + str(user_id)
    
    # Unused variable
    unused_var = "this is not used"
    
    return query

# This file can be used to test the git diff functionality
# Make some changes to this file, stage them, and test the commit hook 

# Test file for validating the new code review functionality

def test_function():
    # This is a test function with some potential issues
    password = "hardcoded_password"  # Security issue
    
    # Inefficient loop
    result = []
    for i in range(1000):
        for j in range(1000):
            result.append(i * j)
    
    # Missing error handling
    file = open("test.txt")
    data = file.read()
    
    return data

def another_function():
    # SQL injection vulnerability
    query = "SELECT * FROM users WHERE id = " + str(user_id)
    
    # Unused variable
    unused_var = "this is not used"
    
    return query

# Test file for validating the new code review functionality

def test_function():
    # This is a test function with some potential issues
    password = "hardcoded_password"  # Security issue
    
    # Inefficient loop
    result = []
    for i in range(1000):
        for j in range(1000):
            result.append(i * j)
    
    # Missing error handling
    file = open("test.txt")
    data = file.read()
    
    return data

def another_function():
    # SQL injection vulnerability
    query = "SELECT * FROM users WHERE id = " + str(user_id)
    
    # Unused variable
    unused_var = "this is not used"
    
    return query

# This file can be used to test the git diff functionality
# Make some changes to this file, stage them, and test the commit hook 

# Test file for validating the new code review functionality

def test_function():
    # This is a test function with some potential issues
    password = "hardcoded_password"  # Security issue
    
    # Inefficient loop
    result = []
    for i in range(1000):
        for j in range(1000):
            result.append(i * j)
    
    # Missing error handling
    file = open("test.txt")
    data = file.read()
    
    return data

def another_function():
    # SQL injection vulnerability
    query = "SELECT * FROM users WHERE id = " + str(user_id)
    
    # Unused variable
    unused_var = "this is not used"
    
    return query